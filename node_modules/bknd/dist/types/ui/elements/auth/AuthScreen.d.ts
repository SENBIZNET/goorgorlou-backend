import { type ReactNode } from "react";
export type AuthScreenProps = {
    method?: "POST" | "GET";
    action?: "login" | "register";
    logo?: ReactNode;
    intro?: ReactNode;
    formOnly?: boolean;
};
export declare function AuthScreen({ method, action, logo, intro, formOnly, }: AuthScreenProps): import("react/jsx-runtime").JSX.Element | null;
