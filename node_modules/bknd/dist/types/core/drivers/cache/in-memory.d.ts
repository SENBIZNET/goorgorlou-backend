import type { ICacheDriver } from "./index";
interface InMemoryCacheOptions {
    maxSize?: number;
    defaultTTL?: number;
}
interface CacheEntry {
    value: string;
    expiresAt: number | null;
    size: number;
}
export declare class InMemoryCacheDriver implements ICacheDriver {
    protected cache: Map<string, CacheEntry>;
    protected maxSize: number;
    protected defaultTTL: number;
    protected currentSize: number;
    constructor(options?: InMemoryCacheOptions);
    protected now(): number;
    protected isExpired(entry: CacheEntry): boolean;
    protected setEntry(key: string, entry: CacheEntry): void;
    get(key: string): Promise<string | undefined>;
    set(key: string, value: string, ttl?: number): Promise<void>;
    del(key: string): Promise<void>;
}
export declare const memoryCache: (options?: InMemoryCacheOptions) => InMemoryCacheDriver;
export {};
