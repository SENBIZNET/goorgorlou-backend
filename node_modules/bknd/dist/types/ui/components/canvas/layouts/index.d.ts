import Dagre from "@dagrejs/dagre";
type Node = {
    id: string;
    width: number;
    height: number;
    x?: number;
    y?: number;
};
type Edge = {
    id: string;
    source: string;
    target: string;
};
export type LayoutProps = {
    nodes: Node[];
    edges: Edge[];
    graph?: Dagre.GraphLabel;
};
export declare const layoutWithDagre: ({ nodes, edges, graph }: LayoutProps) => {
    nodes: {
        x: number;
        y: number;
        id: string;
        width: number;
        height: number;
    }[];
    edges: Edge[];
};
export {};
