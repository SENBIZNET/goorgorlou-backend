import type { MaybePromise } from "../..";
import type { Event } from "./Event";
import type { EventClass } from "./EventManager";
export declare const ListenerModes: readonly ["sync", "async"];
export type ListenerMode = (typeof ListenerModes)[number];
export type ListenerHandler<E extends Event<any, any>> = (event: E, slug: string) => E extends Event<any, infer R> ? MaybePromise<R | void> : never;
export declare class EventListener<E extends Event = Event> {
    mode: ListenerMode;
    event: EventClass;
    handler: ListenerHandler<E>;
    once: boolean;
    id?: string;
    constructor(event: EventClass, handler: ListenerHandler<E>, mode?: ListenerMode, id?: string);
}
