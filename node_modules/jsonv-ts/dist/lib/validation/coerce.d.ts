import { Resolver } from "./resolver";
import type { Schema } from "../schema/schema";
export type CoercionOptions = {
    dropUnknown?: boolean;
    resolver?: Resolver;
    depth?: number;
};
export declare function coerce(s: Schema, _value: unknown, opts?: CoercionOptions): unknown;
