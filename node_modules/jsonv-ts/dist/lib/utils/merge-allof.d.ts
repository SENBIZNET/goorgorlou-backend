export interface MergeOptions {
    /** Recursively merge nested allOfs? (default true) */
    deep?: boolean;
    /** Custom per‑keyword merge resolvers */
    resolvers?: Partial<Record<string, Resolver>>;
}
export type Resolver = (fragments: unknown[], key: string, merge: (a: unknown, b: unknown) => unknown) => unknown;
export declare function mergeAllOf<T = any>(schema: T, opts?: MergeOptions): T;
