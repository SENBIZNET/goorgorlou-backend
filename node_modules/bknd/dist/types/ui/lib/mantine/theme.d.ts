import { createTheme } from "@mantine/core";
export declare function createMantineTheme(scheme: "light" | "dark"): {
    theme: ReturnType<typeof createTheme>;
    forceColorScheme: "light" | "dark";
};
