import { Task } from "../Task";
import { s } from "bknd/utils";
export declare class LogTask extends Task<typeof LogTask.schema> {
    type: string;
    static schema: s.ObjectSchema<{
        readonly delay: s.Schema<{
            readonly default: 10;
        }, number, number> & {
            readonly default: 10;
        };
    }, s.Merge<s.IObjectOptions & {
        additionalProperties: false;
    }>>;
    execute(): Promise<boolean>;
}
