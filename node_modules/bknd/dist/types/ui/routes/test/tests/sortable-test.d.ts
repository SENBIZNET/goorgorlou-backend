import { type DraggableProvided, type DraggableRubric, type DraggableStateSnapshot, type DroppableProps } from "@hello-pangea/dnd";
import type React from "react";
export default function SortableTest(): import("react/jsx-runtime").JSX.Element;
type SortableItemProps<Item = any> = {
    item: Item;
    dnd: {
        provided: DraggableProvided;
        snapshot: DraggableStateSnapshot;
        rubic: DraggableRubric;
    };
};
type SortableListProps<Item = any> = {
    data: Item[];
    extractId?: (item: Item) => string;
    renderItem?: (props: SortableItemProps<Item>) => React.ReactNode;
    dndProps?: Omit<DroppableProps, "children">;
    onReordered?: (from: number, to: number) => void;
    onChange?: (data: Item[]) => void;
};
export declare function SortableList({ data, extractId, renderItem, dndProps, onReordered, onChange, }: SortableListProps): import("react/jsx-runtime").JSX.Element;
export {};
