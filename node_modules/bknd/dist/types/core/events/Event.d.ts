export type EventClass = {
    new (params: any): Event<any, any>;
    slug: string;
};
export declare abstract class Event<Params = any, Returning = void> {
    _returning: Returning;
    /**
     * Unique event slug
     * Must be static, because registering events is done by class
     */
    static slug: string;
    params: Params;
    returned: boolean;
    /**
     * Shallow validation of the event return
     * It'll be deeply validated on the place where it is called
     */
    validate(value: Returning): Event<Params, Returning> | void;
    protected clone<This extends Event<Params, Returning> = Event<Params, Returning>>(this: This, params: Params): This;
    constructor(params: Params);
}
export declare class NoParamEvent extends Event<null> {
    static slug: string;
    constructor();
}
export declare class InvalidEventReturn extends Error {
    constructor(expected: string, given: string);
}
export declare class EventReturnedWithoutValidation extends Error {
    data: any;
    constructor(event: EventClass, data: any);
}
