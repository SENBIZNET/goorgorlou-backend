import { Connection, type DbFunctions, type FieldSpec, type SchemaResponse, type ConnQuery, type ConnQueryResults } from "../Connection";
import { ParseJSONResultsPlugin, type Kysely, type KyselyPlugin, type SelectQueryBuilder } from "kysely";
export type QB = SelectQueryBuilder<any, any, any>;
export declare const plugins: ParseJSONResultsPlugin[];
export declare abstract class PostgresConnection<Client = unknown> extends Connection<Client> {
    protected readonly supported: {
        batching: boolean;
        softscans: boolean;
    };
    constructor(kysely: Kysely<any>, fn?: Partial<DbFunctions>, _plugins?: KyselyPlugin[]);
    getFieldSchema(spec: FieldSpec): SchemaResponse;
    executeQueries<O extends ConnQuery[]>(...qbs: O): Promise<ConnQueryResults<O>>;
}
