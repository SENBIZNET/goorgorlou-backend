import type { IconType } from "react-icons";
import type { ReactNode } from "react";
export type StepTemplate = {
    id: string;
    title: string;
    description: string;
    Icon: IconType;
};
declare const Templates: [() => ReactNode, StepTemplate][];
export default Templates;
