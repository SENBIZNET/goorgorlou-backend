import { s, Tool } from "bknd/utils";
import { McpSchemaHelper, type McpSchema, type SchemaWithMcpOptions } from "./McpSchemaHelper";
type RecordToolAdditionalOptions = {
    get?: boolean;
    add?: boolean;
    update?: boolean;
    remove?: boolean;
};
export interface RecordToolSchemaOptions extends s.IRecordOptions, SchemaWithMcpOptions<RecordToolAdditionalOptions> {
}
export declare class RecordToolSchema<AP extends s.Schema, O extends RecordToolSchemaOptions = RecordToolSchemaOptions> extends s.RecordSchema<AP, O> implements McpSchema {
    constructor(name: string, ap: AP, options?: RecordToolSchemaOptions, new_schema?: s.Schema);
    get mcp(): McpSchemaHelper<RecordToolAdditionalOptions>;
    private getNewSchema;
    private toolGet;
    private toolAdd;
    private toolUpdate;
    private toolRemove;
    getTools(node: s.Node<RecordToolSchema<AP, O>>): Tool<any, any, any>[];
}
export declare const $record: <const AP extends s.Schema, const O extends RecordToolSchemaOptions>(name: string, ap: AP, options?: s.StrictOptions<RecordToolSchemaOptions, O>, new_schema?: s.Schema) => RecordToolSchema<AP, O>;
export {};
