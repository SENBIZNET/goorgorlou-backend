import type { TestRunner } from "../../../core/test";
import type { ICacheDriver } from "./index";
export declare function cacheDriverTestSuite(testRunner: TestRunner, { makeCache, setTime, options, }: {
    makeCache: () => ICacheDriver;
    setTime: (ms: number) => void;
    options?: {
        minTTL?: number;
        skipTTL?: boolean;
    };
}): void;
