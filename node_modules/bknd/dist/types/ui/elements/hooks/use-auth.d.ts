import type { AppAuthSchema } from "../../../auth/auth-schema";
type AuthStrategyData = Pick<AppAuthSchema, "strategies" | "basepath">;
export declare const useAuthStrategies: (options?: {
    baseUrl?: string;
}) => Partial<AuthStrategyData> & {
    loading: boolean;
};
export {};
