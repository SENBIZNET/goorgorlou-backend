import * as s from "jsonv-ts";
import { RpcMessage, type TRpcRequest } from "../rpc";
export declare class PingMessage extends RpcMessage {
    method: string;
    params: s.RecordSchema<s.Schema<s.IAnyOptions, any, any> & s.IAnyOptions, s.IRecordOptions>;
    respond(message: TRpcRequest): Promise<import("..").TRpcResponse<{}>>;
}
