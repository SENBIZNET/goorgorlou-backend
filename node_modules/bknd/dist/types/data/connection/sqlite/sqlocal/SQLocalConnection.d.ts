import { SqliteConnection } from "../SqliteConnection";
import type { SQLocalKysely } from "sqlocal/kysely";
export declare class SQLocalConnection extends SqliteConnection<SQLocalKysely> {
    private connected;
    constructor(client: SQLocalKysely);
    init(): Promise<void>;
}
export declare function sqlocal(instance: InstanceType<typeof SQLocalKysely>): SQLocalConnection;
