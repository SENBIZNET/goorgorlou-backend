export type Constructor<T> = new (...args: any[]) => T;
export type RegisterFn<Item> = (unknown: any) => Item;
export declare class Registry<Item, Items extends Record<string, Item> = Record<string, Item>, Fn extends RegisterFn<Item> = RegisterFn<Item>> {
    private registerFn?;
    private is_set;
    private items;
    constructor(registerFn?: Fn | undefined);
    set<Actual extends Record<string, Item>>(items: Actual): Registry<Item, Actual, Fn>;
    add(name: string, item: Item): this;
    register(name: string, specific: Parameters<Fn>[0]): this;
    get<Name extends keyof Items>(name: Name): Items[Name];
    has(name: keyof Items): boolean;
    all(): Items;
}
