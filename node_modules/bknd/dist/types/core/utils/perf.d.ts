export declare class Perf {
    private marks;
    private startTime;
    private endTime;
    private constructor();
    static start(): Perf;
    mark(markName: string): void;
    close(): void;
    result(): {
        total: number;
        marks: {
            mark: string;
            time: number;
        }[];
    };
    static execute(fn: () => Promise<any>, times?: number): Promise<any>;
}
