import { McpServer } from "../server";
export type StdioTransportOptions = {
    stdin: NodeJS.ReadStream;
    stdout: NodeJS.WriteStream;
    stderr: NodeJS.WriteStream;
    raw?: unknown;
};
export declare function stdioTransport(server: McpServer, opts: StdioTransportOptions): ReadBuffer;
declare class ReadBuffer {
    stdin: NodeJS.ReadStream;
    opts?: {
        onMessage?: (message: any) => void | Promise<void>;
        onError?: (error: any) => void | Promise<void>;
    } | undefined;
    private _buffer?;
    constructor(stdin: NodeJS.ReadStream, opts?: {
        onMessage?: (message: any) => void | Promise<void>;
        onError?: (error: any) => void | Promise<void>;
    } | undefined);
    onData(data: any): void;
    onError(error: any): void;
    append(chunk: Buffer): void;
    readMessage(): any | null;
    [Symbol.dispose](): void;
}
export {};
