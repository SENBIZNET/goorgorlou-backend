import type { IssuerConfig } from "../OAuthStrategy";
type GithubUserInfo = {
    id: number;
    sub: string;
    name: string;
    email: null;
    avatar_url: string;
};
export declare const github: IssuerConfig<GithubUserInfo>;
export {};
