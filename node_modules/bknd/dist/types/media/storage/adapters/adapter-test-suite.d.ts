import { type TestRunner } from "../../../core/test";
import type { StorageAdapter } from "../../../media/storage/StorageAdapter";
import type { BunFile } from "bun";
export declare function adapterTestSuite(testRunner: TestRunner, _adapter: StorageAdapter | (() => StorageAdapter), file: File | BunFile, opts?: {
    retries?: number;
    retryTimeout?: number;
    skipExistsAfterDelete?: boolean;
    testRange?: boolean;
}): Promise<void>;
