import type { Task, TaskResult } from "../tasks/Task";
import { type Condition, TaskConnection } from "../tasks/TaskConnection";
import type { Flow } from "./Flow";
export declare class FlowTaskConnector {
    flow: Flow;
    source: Task;
    constructor(flow: Flow, source: Task);
    private task;
    asInputFor(target: Task, condition?: Condition, max_retries?: number): void;
    asOutputFor(target: Task, condition?: Condition): void;
    getNext(): Task<import("jsonv-ts").Schema<import("jsonv-ts").ISchemaOptions, unknown, unknown>, unknown>[];
    getDepth(): number;
    getInConnections(lower_only?: boolean): TaskConnection[];
    getInTasks(lower_only?: boolean): Task[];
    getOutConnections(result?: TaskResult): TaskConnection[];
    getOutTasks(result?: TaskResult): Task[];
}
