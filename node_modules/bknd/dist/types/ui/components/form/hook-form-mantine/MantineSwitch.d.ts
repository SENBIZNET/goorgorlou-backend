import { type SwitchProps as $SwitchProps } from "@mantine/core";
import { type FieldValues, type UseControllerProps } from "react-hook-form";
export type SwitchProps<T extends FieldValues> = UseControllerProps<T> & Omit<$SwitchProps, "value" | "checked" | "defaultValue">;
export declare function MantineSwitch<T extends FieldValues>({ name, control, defaultValue, rules, shouldUnregister, onChange, ...props }: SwitchProps<T>): import("react/jsx-runtime").JSX.Element;
