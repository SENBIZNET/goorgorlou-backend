import { type SegmentedControlProps as $SegmentedControlProps } from "@mantine/core";
import { type FieldValues, type UseControllerProps } from "react-hook-form";
export type MantineSegmentedControlProps<T extends FieldValues> = UseControllerProps<T> & Omit<$SegmentedControlProps, "values" | "defaultValues"> & {
    label?: string;
    description?: string;
    error?: string;
};
export declare function MantineSegmentedControl<T extends FieldValues>({ name, control, defaultValue, rules, shouldUnregister, onChange, label, size, description, error, ...props }: MantineSegmentedControlProps<T>): import("react/jsx-runtime").JSX.Element;
