import type { s } from "bknd/utils";
export type SettingsNewModalProps = {
    schema: s.ObjectSchema;
    uiSchema?: object;
    anyOfValues?: Record<string, {
        label: string;
        icon?: any;
    }>;
    path: string[];
    prefixPath: string;
    generateKey?: string | ((formData: any) => string);
};
export declare const SettingNewModal: ({ schema, uiSchema, anyOfValues, path, prefixPath, generateKey, }: SettingsNewModalProps) => import("react/jsx-runtime").JSX.Element;
