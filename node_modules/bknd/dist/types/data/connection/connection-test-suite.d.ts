import type { TestRunner } from "../../core/test";
import { Connection } from "./Connection";
import type { MaybePromise } from "../../core/types";
export declare function connectionTestSuite(testRunner: TestRunner, { makeConnection, rawDialectDetails, disableConsoleLog: _disableConsoleLog, }: {
    makeConnection: () => MaybePromise<{
        connection: Connection;
        dispose: () => MaybePromise<void>;
    }>;
    rawDialectDetails: string[];
    disableConsoleLog?: boolean;
}): void;
