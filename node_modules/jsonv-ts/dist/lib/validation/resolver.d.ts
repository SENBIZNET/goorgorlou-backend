import type { Schema } from "../schema/schema";
export declare class Resolver {
    readonly root: Schema;
    private cache;
    constructor(root: Schema);
    hasRef<S extends Schema>(s: S, value: unknown): s is S & {
        $ref: string;
    };
    resolve(ref: string): Schema;
}
