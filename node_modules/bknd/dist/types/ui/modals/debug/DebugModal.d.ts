import type { ContextModalProps } from "@mantine/modals";
import type { ComponentProps } from "react";
import { JsonViewer } from "../../components/code/JsonViewer";
type JsonViewerProps = Omit<ComponentProps<typeof JsonViewer>, "title" | "json">;
type Primitive = object | string | number | boolean | any[];
type DebugProps = {
    data: {
        [key: string]: ({
            label: string;
            value: Primitive;
        } & JsonViewerProps) | Primitive;
    };
} & JsonViewerProps;
export declare function DebugModal({ innerProps }: ContextModalProps<DebugProps>): import("react/jsx-runtime").JSX.Element;
export declare namespace DebugModal {
    var defaultTitle: boolean;
    var modalProps: {
        withCloseButton: false;
        size: "lg";
        classNames: {
            body: string;
        };
    };
}
export {};
