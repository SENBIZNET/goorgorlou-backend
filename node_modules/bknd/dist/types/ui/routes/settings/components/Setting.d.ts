import { type s } from "bknd/utils";
import { type ReactNode } from "react";
import { type SettingsNewModalProps } from "./SettingNewModal";
export type SettingProps<Schema extends s.ObjectSchema = s.ObjectSchema, Props = Schema extends s.ObjectSchema<infer TProperties> ? TProperties : any> = {
    schema: Schema;
    config: any;
    prefix?: string;
    path?: string[];
    uiSchema?: any;
    options?: {
        allowDelete?: (config: any) => boolean;
        allowEdit?: (config: any) => boolean;
        showAlert?: (config: any) => string | ReactNode | undefined;
        reloadOnSave?: boolean;
    };
    properties?: {
        [key in keyof Partial<Props>]: {
            extract?: boolean;
            hide?: boolean;
            new?: Pick<SettingsNewModalProps, "schema" | "generateKey" | "uiSchema" | "anyOfValues">;
            tableValues?: (config: any) => Record<string, any>[];
        };
    };
};
export declare function Setting<Schema extends s.ObjectSchema = s.ObjectSchema>({ schema, uiSchema, config, prefix, options, path, properties, }: SettingProps<Schema>): import("react/jsx-runtime").JSX.Element;
