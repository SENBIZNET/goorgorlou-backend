import type { JSONSchema } from "json-schema-to-ts";
import { type s } from "bknd/utils";
export declare function extractSchema<Schema extends s.ObjectSchema, Keys extends keyof Schema["properties"], Config extends s.Static<Schema>>(schema: Schema, config: Config, keys: Keys[]): [
    Exclude<JSONSchema, boolean | null | undefined>,
    Partial<Config>,
    {
        [K in Keys]: {
            config: Config[K];
            schema: Schema["properties"][K];
        };
    }
];
