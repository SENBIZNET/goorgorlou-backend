import type { TestRunner } from "../core/test";
import type { BkndConfig, DefaultArgs } from "./index";
import type { App } from "../App";
export declare function adapterTestSuite<Config extends BkndConfig = BkndConfig, Args extends DefaultArgs = DefaultArgs>(testRunner: TestRunner, { makeApp, makeHandler, label, overrides, }: {
    makeApp: (config: Config, args?: Args) => Promise<App>;
    makeHandler?: (config?: Config, args?: Args) => (request: Request) => Promise<Response>;
    label?: string;
    overrides?: {
        dbUrl?: string;
    };
}): void;
